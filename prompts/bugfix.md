# Bug 修复指令

```
修复 {{现象描述，附报错信息/复现步骤/相关日志}}

要求：
1. 先定位根因（写进进度日志：现象→根因→修复→验证）
2. 修复后补回归测试，证明不会复发
3. 如果暴露了规范问题（安全/可靠性），同步更新规范或铁律
4. 更新 MEMORY.md 与 FILE_INDEX
```

示例：

```
修复登录接口偶发 500：并发登录时偶尔报 "Connection is closed"，日志在
docs/04-progress/；先找根因再改，补并发测试，别只加 try-catch。
```
