# 新功能指令

```
给 {{项目}} 增加功能：{{功能描述，含入口/行为/期望结果}}

要求：
- 遵循项目规范（AGENTS.md + standards/）
- 契约先行，先定义接口再实现
- 带单元测试和必要的集成测试，证据写入 docs/05-testing/
- 同步更新 FILE_INDEX / MEMORY / 进度日志
- 完成后运行质量门禁
```

示例：

```
给订单服务增加导出 CSV 接口：支持按时间范围导出订单，字段固定，10 万行以内，
返回文件流；超过 10 万行提示用异步任务。带测试和文档。
```
