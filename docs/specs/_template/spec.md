---
title: "SPEC-<id>: <特性名>"
type: spec
status: draft          # draft → reviewing → confirmed（用户确认前实现冻结）
version: 0.1.0
date: 2026-09-17
---

# SPEC-<id> <特性名>

> 确认记录：（用户确认原文/时间；未确认前状态保持 draft）

## 1 背景与目标

为什么做；解决什么问题。（可引用用户原话作为需求原文）

## 2 范围

- **吸收/复用**：哪些既有 DSH 机制直接复用（不重造轮子，机制名写清楚）
- **自研范围**：本插件真正要实现的部分
- **非目标**：明确不做的事（防蔓延）

## 3 形态与交互

功能结构、UI 入口位置与 order（哪个面板/卡片/快捷键）、数据流（Host↔Client 事件流）。

## 4 验收标准

| # | 验收项 | 验证方式 | 在哪种实例验证 |
|---|--------|----------|----------------|
| A1 | … | 命令/UI 操作 | 临时实例 3084 / 3082 |

> 验收必须在**临时实例**完成；通过用户验收后才允许触碰生产实例。

## 5 迁移与运维

阶段顺序、状态持久化位置（`$DSH_HOME/profiles/<profile>/<plugin>/`）、卸载/回滚方式、验证窗口。

## 6 待确认决策点

| # | 决策点 | 候选 | 推荐 | 状态 |
|---|--------|------|------|------|
| 1 | 命名/入口/卸载时机/验证方式… | A / B | A | 待确认 / ✅ |

## 7 依赖与风险

技术风险（原生模块/鉴权门禁/主题/性能）、与既有插件的兼容性。
