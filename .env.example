# 环境变量示例 — 复制为 .env 并填入真实值
# 安全默认值: 密钥/密码一律留空; 服务检测到空值必须拒绝启动 (fail-closed)

# 服务
APP_PORT=8080
APP_ENV=development

# 凭证（生产环境必填）
APP_ADMIN_TOKEN=
DB_PASSWORD=
REDIS_PASSWORD=

# 数据库
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=app
DB_USER=app

# 可观测
LOG_LEVEL=INFO
